[pytest]
addopts = -q --maxfail=1
asyncio_mode = auto
testpaths = tests
markers =
    integration: marks tests that require a real PostgreSQL database (set TEST_DATABASE_URL)
filterwarnings =
    ignore::DeprecationWarning

